harnessApiVersion: '1.0'
type: AWS_LAMBDA
defaults:
  memorySize: 128
  runtime: nodejs8.10
  timeout: 3
functions:
- functionName: ${app.name}_${service.name}_${env.name}_my-function
  handler: index.handler
  memorySize: 128
  runtime: nodejs8.10
  timeout: 3
